#include "main.h"

/**
 * abs - _absolute value
 * Return : int
 * @n : haracter
 * Description: func that return absolute value of a number
 */

int _abs(int n)
{
	return ((n < 0) ? (n * (-1)) : (n));
}
