#include "main.h"

/**
 * _abs - _abs function
 * Return: int
 * @n: n character for absolute value
 * Description: func that return absolute value of a number
 */

int _abs(int n)
{
	return ((n < 0) ? (n * (-1)) : (n));
}
